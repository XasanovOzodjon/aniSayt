# One Master per dub language; Renditions are derived

An Admin uploads a full Master per spoken language (UZ dub, RU dub). ffmpeg builds the quality ladder (360p–1080p HLS). The player switches Rendition in real time from the master playlist. We rejected per-quality manual uploads (too much admin work) and a single mixed-language file (the User must pick a dub).
